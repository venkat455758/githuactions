name: Github actios on deploy
on:
  workflow_dispatch:
    inputs:
      environment:
        description: choose them the default
jobs:
  input-demo:
    runs-on: ubuntu-latest
    steps:
      - name: deploy
        run: echo "deploy the app ${{ inputs.environment }}"

