name: Github actios
on: workflow_dispatch
env:
  APP_ENV: "dev"
jobs:
  env_demo:
  env:
    APP_ENV: "Prod"
    runs-on: ubuntu-latest
    steps:
      - name: connect
        env:
          APP_ENV: "stage"
        run: echo "arr is $APP_ENV
